"use client";

import React from "react";
import ContentBox from "@/components/common/contentBox";

const Settings = () => {
    return (
        <ContentBox pageName="Settings" showExportButton={false} />
    );
};

export default Settings;