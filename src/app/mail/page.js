"use client";

import React from "react";
import ContentBox from "@/components/common/contentBox";

const Mail = () => {
    return (
        <ContentBox pageName="Mail" showExportButton={false} />
    );
};

export default Mail;